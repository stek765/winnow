# winnow — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Costruire un raccoglitore che legge i post Instagram salvati, ne estrae le entità dalle slide, le verifica alla fonte e scrive un file di findings che una sessione Claude Code trasforma in recap settimanale.

**Architecture:** Due motori separati. Il **raccoglitore** (questo piano) è uno script Python schedulabile: Playwright naviga la sessione Instagram, Claude Haiku 4.5 legge le slide, GitHub/HuggingFace verificano le entità, l'output è JSON su disco. Il **giudice** non è codice: è una sessione Claude Code che legge quel JSON con il profilo dell'utente nel contesto. La logica pura (stato, budget, parsing, normalizzazione) è separata dall'I/O in modo che sia testabile senza rete né API.

**Tech Stack:** Python 3.11+ · Playwright (sync API) · anthropic SDK · httpx · pytest · tomllib (stdlib)

**Spec:** `docs/superpowers/specs/2026-08-20-winnow-design.md`

## Global Constraints

- **Python ≥ 3.11** — `tomllib` è nella stdlib solo da 3.11.
- **Codice e identificatori in inglese.** Spec e piano restano in italiano. Il progetto è destinato all'open source (spec §10): identificatori italiani sono attrito per chiunque non sia l'autore.
- **Nessun dato personale nel codice.** Username, URL di cartelle, chiavi API vivono solo in `config.toml`, che è in `.gitignore`. Un test lo verifica (Task 1).
- **Nessuna chiave API scritta su file dal codice.** La chiave si legge da `ANTHROPIC_API_KEY` nell'ambiente. Il codice non la stampa, non la logga, non la scrive.
- **Modello per l'estrazione:** `claude-haiku-4-5` — scelto per costo, unico punto del sistema che consuma token a volume.
- **Prezzi Haiku 4.5 (fissati 2026-08-20):** input $1.00 / 1M token, output $5.00 / 1M token.
- **Soglie di spesa:** avviso a €3/settimana, arresto definitivo a €10/settimana (spec §9).
- **Il raccoglitore non giudica mai.** Estrae e verifica. Nessuna valutazione di merito nel codice.
- **Ogni fallimento di rete o sessione si ferma e lo dice.** Mai ritentare in loop, mai spacciare per verificato ciò che non lo è.
- **Tasso di cambio:** le soglie sono in EUR, i prezzi API in USD. Usare un tasso fisso in config (`eur_per_usd`), non una chiamata di rete: un freno di sicurezza non deve dipendere dalla rete.

## File Structure

| File | Responsabilità |
|---|---|
| `winnow/config.py` | Carica e valida `config.toml`. Nessun default hardcoded che contenga dati personali. |
| `winnow/state.py` | `seen.json`: quali shortcode sono già stati processati. |
| `winnow/budget.py` | `spend.json` + `HALTED`: contabilità e freno d'emergenza. Separato da `state.py` perché ha un ciclo di vita e una criticità diversi — è l'unico modulo che può fermare tutto. |
| `winnow/browser.py` | Playwright: sessione persistente, elenco shortcode, cattura slide, caption. |
| `winnow/extract.py` | Haiku 4.5 vision: slide → entità strutturate. Prompt e parsing sono funzioni pure. |
| `winnow/verify.py` | GitHub / HuggingFace / llmfit. Normalizzazione nomi separata dalle chiamate di rete. |
| `winnow/run.py` | Orchestrazione: mette in fila i moduli sopra e scrive `findings/`. |
| `winnow/cli.py` | `winnow collect | status | reset-halt` |

**Nota di scostamento dalla spec:** la spec §11 metteva `seen.json`, `spend.json` e `HALTED` tutti in `state.py`. Il piano li separa in `state.py` e `budget.py`. Motivo: il budget è l'unico modulo che può arrestare il sistema, e va potuto leggere e rivedere da solo.

---

### Task 1: Scaffolding, config e igiene del repository

**Files:**
- Create: `pyproject.toml`
- Create: `config.example.toml`
- Create: `winnow/__init__.py`
- Create: `winnow/config.py`
- Create: `profiles/esempio.md`
- Test: `tests/test_config.py`
- Test: `tests/test_repo_hygiene.py`

**Interfaces:**
- Consumes: niente (primo task)
- Produces:
  - `Folder` dataclass: `name: str`, `url: str`, `active: bool`, `kind: str`
  - `Limits` dataclass: `warn_eur_week: float`, `halt_eur_week: float`, `posts_per_run: int`, `max_slides: int`, `eur_per_usd: float`
  - `Config` dataclass: `username: str`, `browser_profile: Path`, `folders: list[Folder]`, `limits: Limits`, `model: str`
  - `load_config(path: Path) -> Config`
  - `active_folders(cfg: Config) -> list[Folder]`

- [ ] **Step 1: Scrivere i test che falliscono**

```python
# tests/test_config.py
import pytest
from pathlib import Path
from winnow.config import load_config, active_folders

SAMPLE = """
[instagram]
username = "tizio"
browser_profile = "browser-profile"

[api]
model = "claude-haiku-4-5"

[limits]
warn_eur_week = 3.0
halt_eur_week = 10.0
posts_per_run = 8
max_slides = 15
eur_per_usd = 0.92

[[folders]]
name = "github"
url = "/tizio/saved/github/111/"
active = true
kind = "repo"

[[folders]]
name = "ai"
url = "/tizio/saved/ai/222/"
active = false
kind = "news"
"""


def test_load_config_reads_all_sections(tmp_path):
    p = tmp_path / "config.toml"
    p.write_text(SAMPLE, encoding="utf-8")
    cfg = load_config(p)
    assert cfg.username == "tizio"
    assert cfg.model == "claude-haiku-4-5"
    assert cfg.limits.halt_eur_week == 10.0
    assert len(cfg.folders) == 2


def test_active_folders_skips_inactive(tmp_path):
    p = tmp_path / "config.toml"
    p.write_text(SAMPLE, encoding="utf-8")
    names = [f.name for f in active_folders(load_config(p))]
    assert names == ["github"]


def test_missing_config_raises_clear_error(tmp_path):
    with pytest.raises(FileNotFoundError) as e:
        load_config(tmp_path / "assente.toml")
    assert "config.example.toml" in str(e.value)


def test_halt_threshold_must_exceed_warn(tmp_path):
    bad = SAMPLE.replace("halt_eur_week = 10.0", "halt_eur_week = 1.0")
    p = tmp_path / "config.toml"
    p.write_text(bad, encoding="utf-8")
    with pytest.raises(ValueError):
        load_config(p)
```

```python
# tests/test_repo_hygiene.py
from pathlib import Path

ROOT = Path(__file__).resolve().parents[1]


def test_real_config_is_gitignored():
    ignored = (ROOT / ".gitignore").read_text(encoding="utf-8")
    for entry in ["config.toml", "state/", "findings/", "browser-profile/"]:
        assert entry in ignored, f"{entry} deve stare in .gitignore"


def test_no_personal_data_in_source():
    """Username e id di cartella vivono solo in config.toml, mai nel codice."""
    for py in (ROOT / "winnow").rglob("*.py"):
        text = py.read_text(encoding="utf-8")
        assert "utente" not in text, f"username trovato in {py}"
        assert "/saved/" not in text, f"URL di cartella trovato in {py}"
```

- [ ] **Step 2: Eseguire i test e verificare che falliscano**

Run: `pytest tests/ -v`
Expected: FAIL con `ModuleNotFoundError: No module named 'winnow.config'`

- [ ] **Step 3: Scrivere `pyproject.toml`**

```toml
[project]
name = "winnow"
version = "0.1.0"
description = "Filter your saved social posts against who you actually are"
requires-python = ">=3.11"
dependencies = [
    "playwright>=1.47",
    "anthropic>=0.40",
    "httpx>=0.27",
]

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[project.scripts]
winnow = "winnow.cli:main"

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
```

- [ ] **Step 4: Scrivere `config.example.toml`**

```toml
# Copia questo file in config.toml e riempilo.
# config.toml e' in .gitignore: non finisce mai su GitHub.

[instagram]
username = "IL_TUO_USERNAME"
# Cartella del profilo browser dedicato. NON il browser di tutti i giorni.
browser_profile = "browser-profile"

[api]
model = "claude-haiku-4-5"

[limits]
warn_eur_week = 3.0     # avviso
halt_eur_week = 10.0    # arresto definitivo
posts_per_run = 8       # quanti post per giro, per tenere un ritmo umano
max_slides = 15         # tetto di slide per post
eur_per_usd = 0.92      # tasso fisso: un freno non deve dipendere dalla rete

# Una sezione per ogni cartella salvata.
# L'URL si legge dalla barra degli indirizzi aprendo la cartella su Instagram.
[[folders]]
name = "github"
url = "/IL_TUO_USERNAME/saved/github/000000000000000/"
active = true
kind = "repo"      # repo | news

[[folders]]
name = "must-rewatch"
url = "/IL_TUO_USERNAME/saved/must-rewatch/000000000000000/"
active = true
kind = "news"
```

- [ ] **Step 5: Implementare `winnow/config.py`**

```python
"""Load and validate config.toml. No personal data may live outside it."""
from __future__ import annotations

import tomllib
from dataclasses import dataclass
from pathlib import Path


@dataclass(frozen=True)
class Folder:
    name: str
    url: str
    active: bool
    kind: str


@dataclass(frozen=True)
class Limits:
    warn_eur_week: float
    halt_eur_week: float
    posts_per_run: int
    max_slides: int
    eur_per_usd: float


@dataclass(frozen=True)
class Config:
    username: str
    browser_profile: Path
    folders: list[Folder]
    limits: Limits
    model: str


def load_config(path: Path) -> Config:
    if not path.exists():
        raise FileNotFoundError(
            f"{path} non trovato. Copia config.example.toml in config.toml "
            "e riempilo."
        )
    raw = tomllib.loads(path.read_text(encoding="utf-8"))

    limits = Limits(**raw["limits"])
    if limits.halt_eur_week <= limits.warn_eur_week:
        raise ValueError(
            "halt_eur_week deve essere maggiore di warn_eur_week: "
            f"{limits.halt_eur_week} <= {limits.warn_eur_week}"
        )

    folders = [Folder(**f) for f in raw["folders"]]
    for f in folders:
        if f.kind not in ("repo", "news"):
            raise ValueError(f"kind sconosciuto per la cartella {f.name!r}: {f.kind!r}")

    return Config(
        username=raw["instagram"]["username"],
        browser_profile=Path(raw["instagram"]["browser_profile"]),
        folders=folders,
        limits=limits,
        model=raw["api"]["model"],
    )


def active_folders(cfg: Config) -> list[Folder]:
    return [f for f in cfg.folders if f.active]
```

- [ ] **Step 6: Scrivere `profiles/esempio.md`**

```markdown
(vedi `profiles/esempio.md` nel repository)
```

- [ ] **Step 7: Eseguire i test e verificare che passino**

Run: `pytest tests/ -v`
Expected: PASS (6 test)

- [ ] **Step 8: Commit**

```bash
git add pyproject.toml config.example.toml winnow/ profiles/ tests/
git commit -m "feat: config, scaffolding e test di igiene del repository"
```

---

### Task 2: Stato — quali post sono già stati visti

**Files:**
- Create: `winnow/state.py`
- Test: `tests/test_state.py`

**Interfaces:**
- Consumes: niente
- Produces:
  - `load_seen(path: Path) -> dict[str, dict]` — shortcode → `{"date": str, "folder": str}`
  - `filter_new(seen: dict[str, dict], shortcodes: list[str]) -> list[str]`
  - `mark_seen(path: Path, shortcodes: list[str], folder: str, today: str) -> None`

- [ ] **Step 1: Scrivere i test che falliscono**

```python
# tests/test_state.py
import json
from winnow.state import load_seen, filter_new, mark_seen


def test_load_seen_on_missing_file_returns_empty(tmp_path):
    assert load_seen(tmp_path / "seen.json") == {}


def test_filter_new_keeps_order_and_drops_known():
    seen = {"AAA": {"date": "2026-08-01", "folder": "github"}}
    assert filter_new(seen, ["AAA", "BBB", "CCC"]) == ["BBB", "CCC"]


def test_filter_new_drops_duplicates_within_the_batch():
    assert filter_new({}, ["AAA", "BBB", "AAA"]) == ["AAA", "BBB"]


def test_mark_seen_persists_and_is_read_back(tmp_path):
    p = tmp_path / "seen.json"
    mark_seen(p, ["AAA", "BBB"], "github", "2026-08-20")
    seen = load_seen(p)
    assert set(seen) == {"AAA", "BBB"}
    assert seen["AAA"] == {"date": "2026-08-20", "folder": "github"}


def test_mark_seen_is_additive(tmp_path):
    p = tmp_path / "seen.json"
    mark_seen(p, ["AAA"], "github", "2026-08-20")
    mark_seen(p, ["BBB"], "must-rewatch", "2026-08-21")
    assert set(load_seen(p)) == {"AAA", "BBB"}


def test_mark_seen_creates_parent_directory(tmp_path):
    p = tmp_path / "state" / "seen.json"
    mark_seen(p, ["AAA"], "github", "2026-08-20")
    assert p.exists()


def test_corrupt_seen_file_does_not_crash(tmp_path):
    """Un seen.json rotto non deve far ripartire tutto da capo in silenzio."""
    p = tmp_path / "seen.json"
    p.write_text("{ questo non e' json", encoding="utf-8")
    try:
        load_seen(p)
    except ValueError as e:
        assert "seen.json" in str(e)
    else:
        raise AssertionError("un file corrotto deve alzare ValueError, non tornare {}")
```

- [ ] **Step 2: Eseguire i test e verificare che falliscano**

Run: `pytest tests/test_state.py -v`
Expected: FAIL con `ModuleNotFoundError: No module named 'winnow.state'`

- [ ] **Step 3: Implementare `winnow/state.py`**

```python
"""Which posts have already been processed."""
from __future__ import annotations

import json
from pathlib import Path


def load_seen(path: Path) -> dict[str, dict]:
    if not path.exists():
        return {}
    try:
        return json.loads(path.read_text(encoding="utf-8"))
    except json.JSONDecodeError as e:
        raise ValueError(
            f"{path.name} e' corrotto ({e}). Non lo sovrascrivo da solo: "
            "riprocessare tutto costerebbe. Controllalo o cancellalo a mano."
        ) from e


def filter_new(seen: dict[str, dict], shortcodes: list[str]) -> list[str]:
    out: list[str] = []
    for code in shortcodes:
        if code not in seen and code not in out:
            out.append(code)
    return out


def mark_seen(path: Path, shortcodes: list[str], folder: str, today: str) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    seen = load_seen(path)
    for code in shortcodes:
        seen[code] = {"date": today, "folder": folder}
    path.write_text(json.dumps(seen, indent=2, sort_keys=True), encoding="utf-8")
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `pytest tests/test_state.py -v`
Expected: PASS (7 test)

- [ ] **Step 5: Commit**

```bash
git add winnow/state.py tests/test_state.py
git commit -m "feat: stato dei post gia' visti, con rifiuto dei file corrotti"
```

---

### Task 3: Budget e freno d'emergenza

Questo è il modulo che può fermare tutto. Va scritto prima di qualunque codice che spenda.

**Files:**
- Create: `winnow/budget.py`
- Test: `tests/test_budget.py`

**Interfaces:**
- Consumes: `Limits` da `winnow.config`
- Produces:
  - `PRICES: dict[str, tuple[float, float]]` — modello → (usd per 1M input, usd per 1M output)
  - `cost_usd(model: str, input_tokens: int, output_tokens: int) -> float`
  - `record_spend(path: Path, usd: float, when: datetime) -> None`
  - `weekly_spend(path: Path, now: datetime) -> float`
  - `is_halted(state_dir: Path) -> bool`
  - `write_halt(state_dir: Path, reason: str, spend_eur: float, when: datetime) -> None`
  - `check_brake(state_dir, spend_path, limits, now) -> str` — ritorna `"ok"` o `"warn"`, alza `Halted` se si deve fermare
  - `class Halted(RuntimeError)`

- [ ] **Step 1: Scrivere i test che falliscono**

```python
# tests/test_budget.py
from datetime import datetime, timedelta
import pytest

from winnow.budget import (
    Halted, cost_usd, record_spend, weekly_spend,
    is_halted, write_halt, check_brake,
)
from winnow.config import Limits

NOW = datetime(2026, 8, 20, 3, 0, 0)
LIMITS = Limits(
    warn_eur_week=3.0, halt_eur_week=10.0,
    posts_per_run=8, max_slides=15, eur_per_usd=0.92,
)


def test_cost_uses_haiku_prices():
    # 1M input = $1.00, 1M output = $5.00
    assert cost_usd("claude-haiku-4-5", 1_000_000, 0) == pytest.approx(1.0)
    assert cost_usd("claude-haiku-4-5", 0, 1_000_000) == pytest.approx(5.0)
    assert cost_usd("claude-haiku-4-5", 20_000, 1_000) == pytest.approx(0.025)


def test_cost_of_unknown_model_raises():
    with pytest.raises(KeyError):
        cost_usd("modello-inventato", 1000, 10)


def test_weekly_spend_sums_last_seven_days_only(tmp_path):
    p = tmp_path / "spend.json"
    record_spend(p, 1.0, NOW - timedelta(days=2))
    record_spend(p, 2.0, NOW - timedelta(days=6))
    record_spend(p, 99.0, NOW - timedelta(days=30))  # fuori finestra
    assert weekly_spend(p, NOW) == pytest.approx(3.0)


def test_weekly_spend_on_missing_file_is_zero(tmp_path):
    assert weekly_spend(tmp_path / "spend.json", NOW) == 0.0


def test_check_brake_returns_ok_under_warning(tmp_path):
    p = tmp_path / "spend.json"
    record_spend(p, 0.50, NOW)  # 0.50 usd -> 0.46 eur
    assert check_brake(tmp_path, p, LIMITS, NOW) == "ok"


def test_check_brake_warns_over_warn_threshold(tmp_path):
    p = tmp_path / "spend.json"
    record_spend(p, 5.0, NOW)  # 4.60 eur, sopra warn=3, sotto halt=10
    assert check_brake(tmp_path, p, LIMITS, NOW) == "warn"


def test_check_brake_halts_over_halt_threshold(tmp_path):
    p = tmp_path / "spend.json"
    record_spend(p, 12.0, NOW)  # 11.04 eur, sopra halt=10
    with pytest.raises(Halted):
        check_brake(tmp_path, p, LIMITS, NOW)
    assert is_halted(tmp_path)


def test_halt_file_survives_and_blocks_every_later_run(tmp_path):
    write_halt(tmp_path, "test", 11.04, NOW)
    p = tmp_path / "spend.json"  # spesa azzerata: non basta
    with pytest.raises(Halted):
        check_brake(tmp_path, p, LIMITS, NOW)


def test_halt_file_explains_itself(tmp_path):
    write_halt(tmp_path, "superata soglia settimanale", 11.04, NOW)
    text = (tmp_path / "HALTED").read_text(encoding="utf-8")
    assert "11.04" in text
    assert "2026-08-20" in text
    assert "cancella questo file" in text.lower()
```

- [ ] **Step 2: Eseguire i test e verificare che falliscano**

Run: `pytest tests/test_budget.py -v`
Expected: FAIL con `ModuleNotFoundError: No module named 'winnow.budget'`

- [ ] **Step 3: Implementare `winnow/budget.py`**

```python
"""Spend accounting and the emergency brake.

This is the only module that can stop the whole system. Keep it boring.
"""
from __future__ import annotations

import json
from datetime import datetime, timedelta
from pathlib import Path

from winnow.config import Limits

HALT_FILE = "HALTED"

# usd per 1M tokens: (input, output). Fissati 2026-08-20.
PRICES: dict[str, tuple[float, float]] = {
    "claude-haiku-4-5": (1.00, 5.00),
    "claude-sonnet-5": (3.00, 15.00),
    "claude-opus-5": (5.00, 25.00),
}


class Halted(RuntimeError):
    """Raised when the run must not proceed. Never caught to retry."""


def cost_usd(model: str, input_tokens: int, output_tokens: int) -> float:
    price_in, price_out = PRICES[model]
    return input_tokens / 1_000_000 * price_in + output_tokens / 1_000_000 * price_out


def record_spend(path: Path, usd: float, when: datetime) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    runs = json.loads(path.read_text(encoding="utf-8")) if path.exists() else []
    runs.append({"ts": when.isoformat(), "usd": round(usd, 6)})
    path.write_text(json.dumps(runs, indent=2), encoding="utf-8")


def weekly_spend(path: Path, now: datetime) -> float:
    if not path.exists():
        return 0.0
    cutoff = now - timedelta(days=7)
    runs = json.loads(path.read_text(encoding="utf-8"))
    return sum(
        r["usd"] for r in runs if datetime.fromisoformat(r["ts"]) >= cutoff
    )


def is_halted(state_dir: Path) -> bool:
    return (state_dir / HALT_FILE).exists()


def write_halt(state_dir: Path, reason: str, spend_eur: float, when: datetime) -> None:
    state_dir.mkdir(parents=True, exist_ok=True)
    (state_dir / HALT_FILE).write_text(
        f"winnow si e' fermato il {when.date()} ({when:%H:%M}).\n\n"
        f"Motivo: {reason}\n"
        f"Spesa registrata negli ultimi 7 giorni: EUR {spend_eur:.2f}\n\n"
        "La spesa attesa e' di circa 0,50 USD a settimana. Una cifra molto piu'\n"
        "alta non e' 'un po' caro': e' un difetto. Controlla seen.json e i log\n"
        "prima di ripartire.\n\n"
        "Per ripartire, cancella questo file a mano. Il programma non lo tocca.\n",
        encoding="utf-8",
    )


def check_brake(
    state_dir: Path, spend_path: Path, limits: Limits, now: datetime
) -> str:
    """Return 'ok' or 'warn'. Raise Halted if the run must not proceed."""
    if is_halted(state_dir):
        raise Halted(
            f"{state_dir / HALT_FILE} esiste. Leggilo e cancellalo a mano "
            "per ripartire."
        )

    spend_eur = weekly_spend(spend_path, now) * limits.eur_per_usd

    if spend_eur >= limits.halt_eur_week:
        write_halt(
            state_dir,
            f"spesa settimanale oltre la soglia di EUR {limits.halt_eur_week:.2f}",
            spend_eur,
            now,
        )
        raise Halted(
            f"Spesa settimanale EUR {spend_eur:.2f} oltre la soglia "
            f"EUR {limits.halt_eur_week:.2f}. Arresto definitivo."
        )

    return "warn" if spend_eur >= limits.warn_eur_week else "ok"
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `pytest tests/test_budget.py -v`
Expected: PASS (9 test)

- [ ] **Step 5: Commit**

```bash
git add winnow/budget.py tests/test_budget.py
git commit -m "feat: contabilita' di spesa e freno d'emergenza con arresto definitivo"
```

---

### Task 4: Browser — sessione, elenco shortcode, rilevamento del logout

**Files:**
- Create: `winnow/browser.py`
- Test: `tests/test_browser_parsing.py`

Le funzioni pure (parsing degli href, riconoscimento della pagina di login) sono testate. La navigazione vera non si testa in automatico: si verifica a mano nello Step 6.

**Interfaces:**
- Consumes: `Config` da `winnow.config`
- Produces:
  - `class SessionExpired(RuntimeError)`
  - `parse_shortcodes(hrefs: list[str]) -> list[str]`
  - `looks_logged_out(page_url: str, page_text: str) -> bool`
  - `open_session(profile_dir: Path)` — context manager, restituisce una `Page` Playwright
  - `list_shortcodes(page, folder_url: str) -> list[str]`

- [ ] **Step 1: Scrivere i test che falliscono**

```python
# tests/test_browser_parsing.py
import pytest
from winnow.browser import parse_shortcodes, looks_logged_out


def test_parse_shortcodes_extracts_post_links():
    hrefs = [
        "/", "/reels/", "/explore/",
        "/p/DcEYSBomGEy/", "/p/DcG0M83DmSS/",
        "/tizio/saved/github/111/",
    ]
    assert parse_shortcodes(hrefs) == ["DcEYSBomGEy", "DcG0M83DmSS"]


def test_parse_shortcodes_handles_absolute_urls_and_query():
    hrefs = ["https://www.instagram.com/p/AbC123/?img_index=3"]
    assert parse_shortcodes(hrefs) == ["AbC123"]


def test_parse_shortcodes_deduplicates_preserving_order():
    hrefs = ["/p/AAA/", "/p/BBB/", "/p/AAA/"]
    assert parse_shortcodes(hrefs) == ["AAA", "BBB"]


def test_parse_shortcodes_ignores_reel_links():
    """I reel hanno /reel/, non /p/. Fuori perimetro: non sono caroselli."""
    assert parse_shortcodes(["/reel/XYZ/", "/p/AAA/"]) == ["AAA"]


def test_looks_logged_out_detects_login_page():
    assert looks_logged_out(
        "https://www.instagram.com/accounts/login/", "Accedi a Instagram"
    )


def test_looks_logged_out_detects_login_text_on_any_url():
    assert looks_logged_out(
        "https://www.instagram.com/tizio/saved/github/111/",
        "Accedi a Instagram\nPassword dimenticata?",
    )


def test_looks_logged_out_is_false_on_a_real_page():
    assert not looks_logged_out(
        "https://www.instagram.com/tizio/saved/github/111/",
        "Solo tu puoi vedere gli elementi che hai salvato",
    )
```

- [ ] **Step 2: Eseguire i test e verificare che falliscano**

Run: `pytest tests/test_browser_parsing.py -v`
Expected: FAIL con `ModuleNotFoundError: No module named 'winnow.browser'`

- [ ] **Step 3: Implementare `winnow/browser.py`**

```python
"""Playwright session against Instagram, using a dedicated browser profile."""
from __future__ import annotations

import random
import re
import time
from contextlib import contextmanager
from pathlib import Path

from playwright.sync_api import sync_playwright

BASE = "https://www.instagram.com"
SHORTCODE_RE = re.compile(r"/p/([A-Za-z0-9_-]+)/?")

# Frasi che compaiono solo sulla pagina di accesso.
LOGGED_OUT_MARKERS = ("Accedi a Instagram", "Log in to Instagram", "Password dimenticata")


class SessionExpired(RuntimeError):
    """The Instagram session is gone. Stop; never retry in a loop."""


def parse_shortcodes(hrefs: list[str]) -> list[str]:
    out: list[str] = []
    for href in hrefs:
        m = SHORTCODE_RE.search(href)
        if m and m.group(1) not in out:
            out.append(m.group(1))
    return out


def looks_logged_out(page_url: str, page_text: str) -> bool:
    if "/accounts/login" in page_url:
        return True
    return any(marker in page_text for marker in LOGGED_OUT_MARKERS)


def human_pause(low: float = 1.5, high: float = 4.0) -> None:
    """Irregular waits. 30 posts in four minutes every night gets noticed."""
    time.sleep(random.uniform(low, high))


@contextmanager
def open_session(profile_dir: Path):
    profile_dir.mkdir(parents=True, exist_ok=True)
    with sync_playwright() as pw:
        context = pw.chromium.launch_persistent_context(
            user_data_dir=str(profile_dir),
            headless=False,  # Instagram tratta male l'headless
            viewport={"width": 1440, "height": 900},
        )
        page = context.pages[0] if context.pages else context.new_page()
        try:
            yield page
        finally:
            context.close()


def _guard_session(page) -> None:
    if looks_logged_out(page.url, page.inner_text("body")):
        raise SessionExpired(
            "Sessione Instagram scaduta. Apri il profilo browser dedicato, "
            "accedi a mano, poi rilancia. Non ritento da solo."
        )


def list_shortcodes(page, folder_url: str) -> list[str]:
    page.goto(BASE + folder_url, wait_until="domcontentloaded")
    human_pause(3.0, 5.0)
    _guard_session(page)
    hrefs = page.eval_on_selector_all(
        "a[href]", "els => els.map(e => e.getAttribute('href'))"
    )
    return parse_shortcodes([h for h in hrefs if h])
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `pytest tests/test_browser_parsing.py -v`
Expected: PASS (7 test)

- [ ] **Step 5: Installare Playwright e il browser**

```bash
pip install -e ".[dev]"
playwright install chromium
```

- [ ] **Step 6: Verifica manuale — accesso una tantum e primo elenco**

```bash
python -c "
from pathlib import Path
from winnow.browser import open_session
with open_session(Path('browser-profile')) as page:
    page.goto('https://www.instagram.com/')
    input('Accedi a mano nella finestra aperta, poi premi INVIO qui...')
"
```

Poi, con `config.toml` compilato:

```bash
python -c "
from pathlib import Path
from winnow.config import load_config, active_folders
from winnow.browser import open_session, list_shortcodes
cfg = load_config(Path('config.toml'))
with open_session(cfg.browser_profile) as page:
    for f in active_folders(cfg):
        print(f.name, list_shortcodes(page, f.url))
"
```

Expected: una lista di shortcode per ogni cartella attiva.
Se stampa `SessionExpired`, il login non è stato salvato nel profilo: rifare lo Step 6.

- [ ] **Step 7: Commit**

```bash
git add winnow/browser.py tests/test_browser_parsing.py
git commit -m "feat: sessione browser dedicata, elenco shortcode, arresto su logout"
```

---

### Task 5: Browser — cattura delle slide e della caption

**Files:**
- Modify: `winnow/browser.py`
- Test: `tests/test_browser_slides.py`

**Interfaces:**
- Consumes: `open_session`, `_guard_session`, `human_pause` dal Task 4
- Produces:
  - `slide_url(shortcode: str, index: int) -> str`
  - `count_slides(page) -> int`
  - `capture_post(page, shortcode, out_dir, max_slides) -> tuple[str, str, list[Path]]` — ritorna `(caption, account, percorsi_png)`
  - `read_meta(page) -> tuple[str, str]` · `parse_meta_caption(content) -> str` · `parse_meta_account(content) -> str`

> ⚠️ **Corretto il 2026-08-20 contro il DOM reale.** Le ipotesi iniziali erano sbagliate: Instagram non usa `<article>` ne' `<h1>`, la caption sta nel meta `og:description` (che da' anche l'account), e **`?img_index=N` non funziona a caricamento pulito** — e' gestito solo lato client, quindi un `goto` fresco atterra sempre sulla slide 1. Si avanza cliccando la freccia `aria-label="Avanti"`. La slide visibile e' l'immagine di **area massima** con `x >= 0`: filtrare per sola larghezza fa passare i post suggeriti (311px).

- [ ] **Step 1: Scrivere il test che fallisce**

```python
# tests/test_browser_slides.py
from winnow.browser import slide_url


def test_slide_url_first_slide_has_no_query():
    assert slide_url("AbC123", 1) == "https://www.instagram.com/p/AbC123/"


def test_slide_url_uses_img_index_from_second_slide():
    assert slide_url("AbC123", 5) == (
        "https://www.instagram.com/p/AbC123/?img_index=5"
    )


def test_slide_url_rejects_index_below_one():
    import pytest
    with pytest.raises(ValueError):
        slide_url("AbC123", 0)
```

- [ ] **Step 2: Eseguire il test e verificare che fallisca**

Run: `pytest tests/test_browser_slides.py -v`
Expected: FAIL con `ImportError: cannot import name 'slide_url'`

- [ ] **Step 3: Implementare in `winnow/browser.py`**

Aggiungere in fondo al file:

```python
# Il contenitore della slide. Instagram cambia le classi: si ancora al ruolo.
MEDIA_SELECTOR = "article div[role='button'], article img"
SLIDE_WAIT_S = 5.0  # Instagram carica solo le slide adiacenti: serve attendere


def slide_url(shortcode: str, index: int) -> str:
    if index < 1:
        raise ValueError(f"l'indice della slide parte da 1, ricevuto {index}")
    if index == 1:
        return f"{BASE}/p/{shortcode}/"
    return f"{BASE}/p/{shortcode}/?img_index={index}"


def count_slides(page) -> int:
    """The carousel dots tell how many slides there are. One dot per slide."""
    dots = page.query_selector_all("div[role='presentation'] div[class] > div")
    n = len(dots)
    return n if n > 0 else 1


def get_caption(page) -> str:
    node = page.query_selector("article h1")
    return node.inner_text() if node else ""


def capture_post(
    page, shortcode: str, out_dir: Path, max_slides: int
) -> tuple[str, list[Path]]:
    out_dir.mkdir(parents=True, exist_ok=True)

    page.goto(slide_url(shortcode, 1), wait_until="domcontentloaded")
    time.sleep(SLIDE_WAIT_S)
    _guard_session(page)

    caption = get_caption(page)
    total = min(count_slides(page), max_slides)

    shots: list[Path] = []
    for i in range(1, total + 1):
        if i > 1:
            page.goto(slide_url(shortcode, i), wait_until="domcontentloaded")
            time.sleep(SLIDE_WAIT_S)
        media = page.query_selector(MEDIA_SELECTOR)
        if media is None:
            continue  # slide non caricata: si annota saltandola, non si insiste
        path = out_dir / f"{shortcode}_{i:02d}.png"
        media.screenshot(path=str(path))
        shots.append(path)
        human_pause(0.8, 2.0)

    return caption, shots
```

- [ ] **Step 4: Eseguire il test e verificare che passi**

Run: `pytest tests/test_browser_slides.py -v`
Expected: PASS (3 test)

- [ ] **Step 5: Verifica manuale su un post vero**

```bash
python -c "
from pathlib import Path
from winnow.config import load_config
from winnow.browser import open_session, capture_post
cfg = load_config(Path('config.toml'))
with open_session(cfg.browser_profile) as page:
    caption, shots = capture_post(page, 'DcEYSBomGEy', Path('/tmp/winnow'), 15)
    print('caption:', caption[:120])
    print('slide catturate:', len(shots))
"
```

Expected: caption non vuota e più di una slide. Aprire due PNG e verificare che il testo sia leggibile.
Se le immagini sono nere: alzare `SLIDE_WAIT_S`.

- [ ] **Step 6: Commit**

```bash
git add winnow/browser.py tests/test_browser_slides.py
git commit -m "feat: cattura slide per URL img_index e lettura caption"
```

---

### Task 6: Estrazione — dalle slide alle entità

**Files:**
- Create: `winnow/extract.py`
- Test: `tests/test_extract.py`

Prompt e parsing sono funzioni pure e si testano. La chiamata API no.

**Interfaces:**
- Consumes: `cost_usd` da `winnow.budget`
- Produces:
  - `Entity` dataclass: `kind: str` (`"repo"|"model"|"platform"|"claim"`), `name: str`, `blurb: str`, `slide: int`
  - `PostExtraction` dataclass: `shortcode: str`, `account: str`, `caption: str`, `entities: list[Entity]`, `usd: float`
  - `SYSTEM_PROMPT: str`
  - `parse_entities(text: str) -> list[Entity]`
  - `extract_post(client, model, shortcode, account, caption, slide_paths) -> PostExtraction`

- [ ] **Step 1: Scrivere i test che falliscono**

```python
# tests/test_extract.py
import pytest
from winnow.extract import Entity, parse_entities, SYSTEM_PROMPT


def test_parse_entities_reads_plain_json():
    text = '[{"kind":"repo","name":"lfnovo/open-notebook","blurb":"NotebookLM open","slide":2}]'
    assert parse_entities(text) == [
        Entity(kind="repo", name="lfnovo/open-notebook", blurb="NotebookLM open", slide=2)
    ]


def test_parse_entities_tolerates_markdown_fences():
    text = '```json\n[{"kind":"model","name":"Qwen3-32B","blurb":"","slide":1}]\n```'
    assert parse_entities(text)[0].name == "Qwen3-32B"


def test_parse_entities_returns_empty_list_for_empty_array():
    assert parse_entities("[]") == []


def test_parse_entities_drops_entries_with_unknown_kind():
    text = '[{"kind":"pesce","name":"x","blurb":"","slide":1},' \
           '{"kind":"repo","name":"a/b","blurb":"","slide":1}]'
    assert [e.name for e in parse_entities(text)] == ["a/b"]


def test_parse_entities_drops_entries_without_a_name():
    text = '[{"kind":"repo","name":"","blurb":"vuoto","slide":1}]'
    assert parse_entities(text) == []


def test_parse_entities_raises_on_non_json():
    with pytest.raises(ValueError):
        parse_entities("mi dispiace, non riesco a leggere l'immagine")


def test_system_prompt_forbids_judging():
    """Il raccoglitore non giudica mai (spec, vincoli globali)."""
    low = SYSTEM_PROMPT.lower()
    assert "do not judge" in low or "non giudicare" in low
```

- [ ] **Step 2: Eseguire i test e verificare che falliscano**

Run: `pytest tests/test_extract.py -v`
Expected: FAIL con `ModuleNotFoundError: No module named 'winnow.extract'`

- [ ] **Step 3: Implementare `winnow/extract.py`**

```python
"""Slides -> structured entities, via a cheap vision model.

This module never judges. It extracts what is written and nothing else.
"""
from __future__ import annotations

import base64
import json
import re
from dataclasses import dataclass
from pathlib import Path

from winnow.budget import cost_usd

KINDS = {"repo", "model", "platform", "claim"}

SYSTEM_PROMPT = """You read slides from a social media carousel and extract, \
verbatim, the concrete things they name.

Return ONLY a JSON array. Each element:
  kind:  "repo" for a code repository (prefer the "owner/name" form when shown),
         "model" for an AI model, "platform" for a product or service,
         "claim" for a factual assertion with no named artifact.
  name:  the exact name as written. Never invent, never expand an acronym.
  blurb: one sentence, from the slide, of what it is. Empty string if absent.
  slide: the 1-based index of the slide it came from.

Rules:
- Extract only what is actually written or visible. If a slide promises items \
but names none, return nothing for it.
- Do not judge quality, usefulness, or credibility. That is not your job.
- If the slides name nothing concrete, return [].
"""

USER_TEMPLATE = (
    "Post by @{account}.\n\nCaption:\n{caption}\n\n"
    "{n} slides follow, in order. Extract the entities."
)


@dataclass(frozen=True)
class Entity:
    kind: str
    name: str
    blurb: str
    slide: int


@dataclass(frozen=True)
class PostExtraction:
    shortcode: str
    account: str
    caption: str
    entities: list[Entity]
    usd: float


def parse_entities(text: str) -> list[Entity]:
    cleaned = re.sub(r"^\s*```(?:json)?|```\s*$", "", text.strip(), flags=re.MULTILINE)
    try:
        data = json.loads(cleaned)
    except json.JSONDecodeError as e:
        raise ValueError(f"risposta non JSON dal modello: {text[:200]!r}") from e
    if not isinstance(data, list):
        raise ValueError(f"attesa una lista, ricevuto {type(data).__name__}")

    out: list[Entity] = []
    for item in data:
        if not isinstance(item, dict):
            continue
        kind, name = item.get("kind"), (item.get("name") or "").strip()
        if kind not in KINDS or not name:
            continue
        out.append(
            Entity(
                kind=kind,
                name=name,
                blurb=(item.get("blurb") or "").strip(),
                slide=int(item.get("slide") or 1),
            )
        )
    return out


def _image_block(path: Path) -> dict:
    return {
        "type": "image",
        "source": {
            "type": "base64",
            "media_type": "image/png",
            "data": base64.standard_b64encode(path.read_bytes()).decode("utf-8"),
        },
    }


def extract_post(
    client,
    model: str,
    shortcode: str,
    account: str,
    caption: str,
    slide_paths: list[Path],
) -> PostExtraction:
    content: list[dict] = [
        {
            "type": "text",
            "text": USER_TEMPLATE.format(
                account=account, caption=caption, n=len(slide_paths)
            ),
        }
    ]
    content.extend(_image_block(p) for p in slide_paths)

    response = client.messages.create(
        model=model,
        max_tokens=4000,
        system=SYSTEM_PROMPT,
        messages=[{"role": "user", "content": content}],
    )
    text = next((b.text for b in response.content if b.type == "text"), "[]")

    return PostExtraction(
        shortcode=shortcode,
        account=account,
        caption=caption,
        entities=parse_entities(text),
        usd=cost_usd(model, response.usage.input_tokens, response.usage.output_tokens),
    )
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `pytest tests/test_extract.py -v`
Expected: PASS (7 test)

- [ ] **Step 5: Verifica manuale su slide vere**

Usare le PNG prodotte nel Task 5 Step 5:

```bash
ANTHROPIC_API_KEY=... python -c "
import anthropic, pathlib
from winnow.extract import extract_post
shots = sorted(pathlib.Path('/tmp/winnow').glob('DcEYSBomGEy_*.png'))
r = extract_post(anthropic.Anthropic(), 'claude-haiku-4-5',
                 'DcEYSBomGEy', 'getintoai', '', shots)
for e in r.entities: print(e.kind, '|', e.name, '|', e.blurb[:60])
print('costo USD:', round(r.usd, 4))
"
```

Expected: fra le entità deve comparire `open-notebook` (o `lfnovo/open-notebook`) e `OpenSEO`. Costo atteso: **sotto $0.05**.
Se il costo supera $0.10 su un post solo, fermarsi: il conto non torna col preventivo di $2/mese.

- [ ] **Step 6: Commit**

```bash
git add winnow/extract.py tests/test_extract.py
git commit -m "feat: estrazione delle entita' dalle slide con Haiku, senza giudizio"
```

---

### Task 7: Verifica — GitHub

**Files:**
- Create: `winnow/verify.py`
- Test: `tests/test_verify_github.py`

**Interfaces:**
- Consumes: `Entity` da `winnow.extract`
- Produces:
  - `Verification` dataclass: `checked: bool`, `exists: bool`, `stars: int | None`, `last_commit: str | None`, `archived: bool | None`, `license: str | None`, `description: str | None`, `url: str | None`, `note: str`
  - `normalize_repo_name(text: str) -> str | None`
  - `verify_repo(http, owner_repo: str) -> Verification`

- [ ] **Step 1: Scrivere i test che falliscono**

```python
# tests/test_verify_github.py
import httpx
import pytest
from winnow.verify import normalize_repo_name, verify_repo


def test_normalize_accepts_owner_slash_name():
    assert normalize_repo_name("lfnovo/open-notebook") == "lfnovo/open-notebook"


def test_normalize_strips_github_urls():
    assert normalize_repo_name(
        "https://github.com/lfnovo/open-notebook"
    ) == "lfnovo/open-notebook"
    assert normalize_repo_name(
        "github.com/lfnovo/open-notebook.git"
    ) == "lfnovo/open-notebook"


def test_normalize_returns_none_for_a_bare_display_name():
    """'Open Notebook' non e' un repo: non si indovina l'owner."""
    assert normalize_repo_name("Open Notebook") is None


def test_normalize_returns_none_for_empty():
    assert normalize_repo_name("   ") is None


def _client(handler):
    return httpx.Client(transport=httpx.MockTransport(handler))


def test_verify_repo_reads_the_fields_that_matter():
    def handler(request):
        assert request.url.path == "/repos/lfnovo/open-notebook"
        return httpx.Response(200, json={
            "stargazers_count": 37000,
            "pushed_at": "2026-08-17T10:00:00Z",
            "archived": False,
            "license": {"spdx_id": "MIT"},
            "description": "Open source NotebookLM alternative",
            "html_url": "https://github.com/lfnovo/open-notebook",
        })
    v = verify_repo(_client(handler), "lfnovo/open-notebook")
    assert v.checked and v.exists
    assert v.stars == 37000
    assert v.license == "MIT"
    assert v.last_commit == "2026-08-17"


def test_verify_repo_marks_a_404_as_checked_and_absent():
    def handler(request):
        return httpx.Response(404, json={"message": "Not Found"})
    v = verify_repo(_client(handler), "tizio/inesistente")
    assert v.checked and not v.exists


def test_network_failure_is_never_reported_as_verified():
    def handler(request):
        raise httpx.ConnectError("rete assente")
    v = verify_repo(_client(handler), "lfnovo/open-notebook")
    assert v.checked is False
    assert "rete" in v.note.lower() or "network" in v.note.lower()


def test_rate_limit_is_not_reported_as_absent():
    def handler(request):
        return httpx.Response(403, json={"message": "API rate limit exceeded"})
    v = verify_repo(_client(handler), "lfnovo/open-notebook")
    assert v.checked is False
    assert v.exists is False or v.exists is None
```

- [ ] **Step 2: Eseguire i test e verificare che falliscano**

Run: `pytest tests/test_verify_github.py -v`
Expected: FAIL con `ModuleNotFoundError: No module named 'winnow.verify'`

- [ ] **Step 3: Implementare `winnow/verify.py`**

```python
"""Check claims against their source. Never report unverified as verified."""
from __future__ import annotations

import re
from dataclasses import dataclass

import httpx

GITHUB_API = "https://api.github.com"
REPO_RE = re.compile(r"^([A-Za-z0-9][\w.-]*)/([\w.-]+?)(?:\.git)?$")


@dataclass(frozen=True)
class Verification:
    checked: bool
    exists: bool | None = None
    stars: int | None = None
    last_commit: str | None = None
    archived: bool | None = None
    license: str | None = None
    description: str | None = None
    url: str | None = None
    note: str = ""


def normalize_repo_name(text: str) -> str | None:
    s = text.strip()
    if not s:
        return None
    s = re.sub(r"^https?://", "", s)
    s = re.sub(r"^(www\.)?github\.com/", "", s)
    s = s.rstrip("/")
    m = REPO_RE.match(s)
    return f"{m.group(1)}/{m.group(2)}" if m else None


def verify_repo(http: httpx.Client, owner_repo: str) -> Verification:
    try:
        r = http.get(
            f"{GITHUB_API}/repos/{owner_repo}",
            headers={"Accept": "application/vnd.github+json"},
            timeout=15.0,
        )
    except httpx.HTTPError as e:
        return Verification(checked=False, note=f"errore di rete: {e}")

    if r.status_code == 404:
        return Verification(checked=True, exists=False, note="repository inesistente")
    if r.status_code != 200:
        return Verification(
            checked=False,
            note=f"GitHub ha risposto {r.status_code} (rate limit?): non verificato",
        )

    d = r.json()
    pushed = (d.get("pushed_at") or "")[:10] or None
    return Verification(
        checked=True,
        exists=True,
        stars=d.get("stargazers_count"),
        last_commit=pushed,
        archived=d.get("archived"),
        license=(d.get("license") or {}).get("spdx_id"),
        description=d.get("description"),
        url=d.get("html_url"),
    )
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `pytest tests/test_verify_github.py -v`
Expected: PASS (8 test)

- [ ] **Step 5: Commit**

```bash
git add winnow/verify.py tests/test_verify_github.py
git commit -m "feat: verifica dei repository su GitHub, con distinzione fra assente e non verificato"
```

---

### Task 8: Verifica — modelli su HuggingFace e fit hardware con llmfit

**Files:**
- Modify: `winnow/verify.py`
- Test: `tests/test_verify_model.py`

**Interfaces:**
- Consumes: `Verification` dal Task 7
- Produces:
  - `verify_model(http, name: str) -> Verification`
  - `llmfit_available() -> bool`
  - `hardware_note(name: str) -> str` — usa `llmfit` se presente, stringa vuota altrimenti

- [ ] **Step 1: Scrivere i test che falliscono**

```python
# tests/test_verify_model.py
import httpx
from winnow.verify import verify_model, hardware_note


def _client(handler):
    return httpx.Client(transport=httpx.MockTransport(handler))


def test_verify_model_finds_an_existing_model():
    def handler(request):
        assert "/api/models" in request.url.path
        return httpx.Response(200, json=[{
            "modelId": "Qwen/Qwen3-32B",
            "downloads": 900000,
            "likes": 1200,
        }])
    v = verify_model(_client(handler), "Qwen3-32B")
    assert v.checked and v.exists
    assert v.description and "Qwen/Qwen3-32B" in v.description


def test_verify_model_reports_absence_when_no_results():
    def handler(request):
        return httpx.Response(200, json=[])
    v = verify_model(_client(handler), "Qwen 27B uncensored")
    assert v.checked and not v.exists
    assert "nessun modello" in v.note.lower()


def test_verify_model_network_failure_is_not_verified():
    def handler(request):
        raise httpx.ConnectError("giu'")
    v = verify_model(_client(handler), "Qwen3-32B")
    assert v.checked is False


def test_hardware_note_is_empty_when_llmfit_is_absent(monkeypatch):
    monkeypatch.setattr("winnow.verify.llmfit_available", lambda: False)
    assert hardware_note("Qwen3-32B") == ""
```

- [ ] **Step 2: Eseguire i test e verificare che falliscano**

Run: `pytest tests/test_verify_model.py -v`
Expected: FAIL con `ImportError: cannot import name 'verify_model'`

- [ ] **Step 3: Implementare in `winnow/verify.py`**

Aggiungere in fondo al file:

```python
import shutil
import subprocess

HF_API = "https://huggingface.co"


def verify_model(http: httpx.Client, name: str) -> Verification:
    try:
        r = http.get(
            f"{HF_API}/api/models",
            params={"search": name, "limit": 3},
            timeout=15.0,
        )
    except httpx.HTTPError as e:
        return Verification(checked=False, note=f"errore di rete: {e}")

    if r.status_code != 200:
        return Verification(
            checked=False, note=f"HuggingFace ha risposto {r.status_code}"
        )

    hits = r.json()
    if not hits:
        return Verification(
            checked=True,
            exists=False,
            note=f"nessun modello su HuggingFace corrisponde a {name!r}",
        )

    top = hits[0]
    return Verification(
        checked=True,
        exists=True,
        stars=top.get("likes"),
        description=top.get("modelId"),
        url=f"{HF_API}/{top.get('modelId')}",
        note=f"{top.get('downloads', 0)} download",
    )


def llmfit_available() -> bool:
    return shutil.which("llmfit") is not None


def hardware_note(name: str) -> str:
    """Ask llmfit whether this model fits the local machine. Best effort."""
    if not llmfit_available():
        return ""
    try:
        out = subprocess.run(
            ["llmfit", "fit", name, "--json"],
            capture_output=True, text=True, timeout=30,
        )
    except (subprocess.SubprocessError, OSError) as e:
        return f"llmfit non eseguibile: {e}"
    return out.stdout.strip() if out.returncode == 0 else out.stderr.strip()[:200]
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `pytest tests/test_verify_model.py -v`
Expected: PASS (4 test)

- [ ] **Step 5: Verifica manuale del claim falso**

```bash
python -c "
import httpx
from winnow.verify import verify_model
print(verify_model(httpx.Client(), 'Qwen 27B'))
print(verify_model(httpx.Client(), 'Gemma 27B'))
"
```

Expected: la prima deve risultare assente o corrispondere a qualcosa di diverso; la seconda deve esistere. È il caso descritto nella spec §7.

- [ ] **Step 6: Commit**

```bash
git add winnow/verify.py tests/test_verify_model.py
git commit -m "feat: verifica dei modelli su HuggingFace e nota di fit hardware via llmfit"
```

---

### Task 9: Orchestrazione — il giro completo e il file di findings

**Files:**
- Create: `winnow/run.py`
- Test: `tests/test_run.py`

**Interfaces:**
- Consumes: tutto dai Task 1-8
- Produces:
  - `enrich(http, entity: Entity) -> Verification`
  - `findings_path(root: Path, day: date) -> Path`
  - `write_findings(path, extractions, verifications, spend_usd) -> None`
  - `collect(cfg, state_dir, findings_dir, shots_dir, client, http, page, now) -> dict` — riassunto del giro

- [ ] **Step 1: Scrivere i test che falliscono**

```python
# tests/test_run.py
import json
from datetime import date, datetime
from pathlib import Path

import httpx
from winnow.extract import Entity, PostExtraction
from winnow.run import enrich, findings_path, write_findings


def test_findings_path_is_one_file_per_day(tmp_path):
    p = findings_path(tmp_path, date(2026, 8, 20))
    assert p == tmp_path / "2026-08-20.json"


def test_enrich_routes_a_repo_entity_to_github():
    def handler(request):
        assert "api.github.com" in str(request.url)
        return httpx.Response(200, json={
            "stargazers_count": 10, "pushed_at": "2026-08-01T00:00:00Z",
            "archived": False, "license": {"spdx_id": "MIT"},
            "description": "x", "html_url": "https://github.com/a/b",
        })
    http = httpx.Client(transport=httpx.MockTransport(handler))
    v = enrich(http, Entity("repo", "a/b", "", 2))
    assert v.checked and v.exists and v.stars == 10


def test_enrich_does_not_call_the_network_for_an_unnormalizable_repo():
    def handler(request):
        raise AssertionError("non deve chiamare la rete")
    http = httpx.Client(transport=httpx.MockTransport(handler))
    v = enrich(http, Entity("repo", "Open Notebook", "", 2))
    assert v.checked is False
    assert "owner/name" in v.note


def test_enrich_leaves_a_claim_unverified_by_source():
    def handler(request):
        raise AssertionError("non deve chiamare la rete")
    http = httpx.Client(transport=httpx.MockTransport(handler))
    v = enrich(http, Entity("claim", "diventa ricco con l'AI", "", 1))
    assert v.checked is False


def test_write_findings_produces_readable_json(tmp_path):
    p = tmp_path / "2026-08-20.json"
    ex = PostExtraction(
        shortcode="AAA", account="tizio", caption="c",
        entities=[Entity("repo", "a/b", "blurb", 2)], usd=0.01,
    )
    from winnow.verify import Verification
    write_findings(p, [ex], {("AAA", "a/b"): Verification(checked=True, exists=True, stars=5)}, 0.01)

    data = json.loads(p.read_text(encoding="utf-8"))
    assert data["spend_usd"] == 0.01
    assert data["posts"][0]["shortcode"] == "AAA"
    assert data["posts"][0]["entities"][0]["verification"]["stars"] == 5
```

- [ ] **Step 2: Eseguire i test e verificare che falliscano**

Run: `pytest tests/test_run.py -v`
Expected: FAIL con `ModuleNotFoundError: No module named 'winnow.run'`

- [ ] **Step 3: Implementare `winnow/run.py`**

```python
"""One collection run: folders -> new posts -> entities -> verifications -> file."""
from __future__ import annotations

import json
from dataclasses import asdict
from datetime import date, datetime
from pathlib import Path

import httpx

from winnow.budget import Halted, check_brake, record_spend
from winnow.browser import capture_post, list_shortcodes
from winnow.config import Config, active_folders
from winnow.extract import Entity, PostExtraction, extract_post
from winnow.state import filter_new, load_seen, mark_seen
from winnow.verify import (
    Verification, hardware_note, normalize_repo_name, verify_model, verify_repo,
)


def enrich(http: httpx.Client, entity: Entity) -> Verification:
    if entity.kind == "repo":
        slug = normalize_repo_name(entity.name)
        if slug is None:
            return Verification(
                checked=False,
                note=f"{entity.name!r} non e' nella forma owner/name: "
                     "non si indovina l'owner",
            )
        return verify_repo(http, slug)

    if entity.kind == "model":
        v = verify_model(http, entity.name)
        note = hardware_note(entity.name)
        if note:
            v = Verification(**{**asdict(v), "note": f"{v.note} | {note}"})
        return v

    # platform e claim: nessuna fonte automatica. Il giudice li valuta a mano.
    return Verification(
        checked=False, note="nessuna fonte automatica per questo tipo"
    )


def findings_path(root: Path, day: date) -> Path:
    return root / f"{day.isoformat()}.json"


def write_findings(
    path: Path,
    extractions: list[PostExtraction],
    verifications: dict[tuple[str, str], Verification],
    spend_usd: float,
) -> None:
    path.parent.mkdir(parents=True, exist_ok=True)
    payload = {
        "spend_usd": round(spend_usd, 6),
        "posts": [
            {
                "shortcode": ex.shortcode,
                "account": ex.account,
                "caption": ex.caption,
                "url": f"https://www.instagram.com/p/{ex.shortcode}/",
                "entities": [
                    {
                        **asdict(e),
                        "verification": asdict(
                            verifications.get(
                                (ex.shortcode, e.name), Verification(checked=False)
                            )
                        ),
                    }
                    for e in ex.entities
                ],
            }
            for ex in extractions
        ],
    }
    path.write_text(json.dumps(payload, indent=2, ensure_ascii=False), encoding="utf-8")


def collect(
    cfg: Config,
    state_dir: Path,
    findings_dir: Path,
    shots_dir: Path,
    client,
    http: httpx.Client,
    page,
    now: datetime,
) -> dict:
    spend_path = state_dir / "spend.json"
    seen_path = state_dir / "seen.json"

    status = check_brake(state_dir, spend_path, cfg.limits, now)  # may raise Halted

    seen = load_seen(seen_path)
    todo: list[tuple[str, str]] = []
    for folder in active_folders(cfg):
        for code in filter_new(seen, list_shortcodes(page, folder.url)):
            todo.append((code, folder.name))
    todo = todo[: cfg.limits.posts_per_run]

    extractions: list[PostExtraction] = []
    verifications: dict[tuple[str, str], Verification] = {}
    spend = 0.0

    for code, folder_name in todo:
        caption, account, shots = capture_post(
            page, code, shots_dir, cfg.limits.max_slides
        )
        ex = extract_post(client, cfg.model, code, account, caption, shots)
        extractions.append(ex)
        spend += ex.usd
        record_spend(spend_path, ex.usd, now)

        for e in ex.entities:
            verifications[(code, e.name)] = enrich(http, e)

        mark_seen(seen_path, [code], folder_name, now.date().isoformat())

    write_findings(findings_path(findings_dir, now.date()), extractions, verifications, spend)

    return {
        "status": status,
        "posts": len(extractions),
        "entities": sum(len(e.entities) for e in extractions),
        "spend_usd": round(spend, 4),
    }
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `pytest tests/test_run.py -v`
Expected: PASS (5 test)

- [ ] **Step 5: Eseguire l'intera suite**

Run: `pytest tests/ -v`
Expected: PASS (tutti i test dei Task 1-9)

- [ ] **Step 6: Commit**

```bash
git add winnow/run.py tests/test_run.py
git commit -m "feat: orchestrazione del giro di raccolta e scrittura dei findings"
```

---

### Task 10: CLI, README e schedulazione

**Files:**
- Create: `winnow/cli.py`
- Create: `README.md`
- Test: `tests/test_cli.py`

**Interfaces:**
- Consumes: tutto dai Task 1-9
- Produces: `main(argv: list[str] | None = None) -> int`

- [ ] **Step 1: Scrivere i test che falliscono**

```python
# tests/test_cli.py
from winnow.cli import main


def test_no_command_prints_usage_and_fails(capsys):
    assert main([]) == 2
    assert "collect" in capsys.readouterr().out


def test_status_reports_halt(tmp_path, capsys):
    (tmp_path / "HALTED").write_text("fermo per test", encoding="utf-8")
    code = main(["status", "--state-dir", str(tmp_path)])
    out = capsys.readouterr().out
    assert code == 1
    assert "HALTED" in out or "fermo" in out


def test_reset_halt_removes_the_file(tmp_path, capsys):
    (tmp_path / "HALTED").write_text("fermo", encoding="utf-8")
    assert main(["reset-halt", "--state-dir", str(tmp_path)]) == 0
    assert not (tmp_path / "HALTED").exists()


def test_reset_halt_when_not_halted_is_a_noop(tmp_path):
    assert main(["reset-halt", "--state-dir", str(tmp_path)]) == 0
```

- [ ] **Step 2: Eseguire i test e verificare che falliscano**

Run: `pytest tests/test_cli.py -v`
Expected: FAIL con `ModuleNotFoundError: No module named 'winnow.cli'`

- [ ] **Step 3: Implementare `winnow/cli.py`**

```python
"""winnow collect | status | reset-halt"""
from __future__ import annotations

import argparse
import sys
from datetime import datetime
from pathlib import Path

from winnow.budget import HALT_FILE, Halted, is_halted, weekly_spend
from winnow.config import load_config


def _parser() -> argparse.ArgumentParser:
    p = argparse.ArgumentParser(prog="winnow")
    p.add_argument("--state-dir", default="state", type=Path)
    p.add_argument("--findings-dir", default="findings", type=Path)
    p.add_argument("--config", default="config.toml", type=Path)
    p.add_argument("command", nargs="?", choices=["collect", "status", "reset-halt"])
    return p


def _cmd_status(args) -> int:
    if is_halted(args.state_dir):
        print((args.state_dir / HALT_FILE).read_text(encoding="utf-8"))
        return 1
    spent = weekly_spend(args.state_dir / "spend.json", datetime.now())
    print(f"attivo · spesa ultimi 7 giorni: USD {spent:.4f}")
    return 0


def _cmd_reset_halt(args) -> int:
    f = args.state_dir / HALT_FILE
    if f.exists():
        f.unlink()
        print(f"{HALT_FILE} rimosso. winnow puo' ripartire.")
    else:
        print("non era fermo.")
    return 0


def _cmd_collect(args) -> int:
    import anthropic
    import httpx

    from winnow.browser import SessionExpired, open_session
    from winnow.run import collect

    cfg = load_config(args.config)
    try:
        with open_session(cfg.browser_profile) as page, httpx.Client() as http:
            summary = collect(
                cfg, args.state_dir, args.findings_dir, args.state_dir / "shots",
                anthropic.Anthropic(), http, page, datetime.now(),
            )
    except Halted as e:
        print(f"ARRESTO: {e}", file=sys.stderr)
        return 1
    except SessionExpired as e:
        print(f"SESSIONE: {e}", file=sys.stderr)
        return 3

    print(
        f"{summary['posts']} post · {summary['entities']} entita' · "
        f"USD {summary['spend_usd']}"
    )
    if summary["status"] == "warn":
        print("ATTENZIONE: spesa settimanale oltre la soglia di avviso.", file=sys.stderr)
    return 0


def main(argv: list[str] | None = None) -> int:
    args = _parser().parse_args(argv)
    if args.command is None:
        _parser().print_help()
        return 2
    return {
        "status": _cmd_status,
        "reset-halt": _cmd_reset_halt,
        "collect": _cmd_collect,
    }[args.command](args)


if __name__ == "__main__":
    raise SystemExit(main())
```

- [ ] **Step 4: Eseguire i test e verificare che passino**

Run: `pytest tests/test_cli.py -v`
Expected: PASS (4 test)

- [ ] **Step 5: Rileggere il `README.md`** (gia' scritto il 2026-08-20) e verificare che comandi e nomi combacino con il codice appena implementato. Il testo di riferimento e' quello in radice, non quello qui sotto.

```markdown
# winnow

Un filtro per i post che salvi sui social. Non li riassume: li **scarta**.

La maggior parte di quello che salvi è clickbait, e l'informazione utile spesso
non è nel testo ma dentro le slide di un carosello. winnow legge le slide,
estrae le cose concrete che nominano (repository, modelli, prodotti), **le
verifica alla fonte**, e ti lascia un file con i fatti. Un post curatissimo che
elenca repository morte da due anni viene scartato. Un post clickbait che ne
contiene una viva da 37k stelle passa.

## La tesi

> La qualità del filtro dipende da quanto bene hai scritto chi sei.

winnow ha due metà. Il **raccoglitore** (questo codice) è generale: naviga,
legge, verifica. Il **giudice** sei tu più un modello, e legge il tuo
`profiles/tuo.md` — obiettivi, decisioni prese, cose che hai già scartato.
Senza quel file winnow è un aggregatore come un altro. Con quel file diventa
tuo.

## Installazione

```bash
pip install -e ".[dev]"
playwright install chromium
cp config.example.toml config.toml   # poi riempilo
export ANTHROPIC_API_KEY=...
```

Il primo accesso si fa a mano, in un profilo browser dedicato (**non** il tuo
browser di tutti i giorni):

```bash
python -c "
from pathlib import Path
from winnow.browser import open_session
with open_session(Path('browser-profile')) as p:
    p.goto('https://www.instagram.com/')
    input('Accedi, poi INVIO...')
"
```

## Uso

```bash
winnow collect      # un giro di raccolta -> findings/YYYY-MM-DD.json
winnow status       # spesa della settimana, o il motivo dell'arresto
winnow reset-halt   # riparte dopo un arresto, deliberatamente
```

## Spesa

Ogni giro costa. Con le impostazioni di default la spesa attesa è di **circa
0,50 USD a settimana**. winnow tiene la contabilità e **si ferma per sempre**
se la spesa settimanale supera la soglia in `config.toml` — non per risparmiare,
ma perché una spesa venti volte il previsto è un difetto, non un prezzo.

⚠️ **Metti anche un limite di spesa sulla chiave API**, nella console del tuo
fornitore. Il freno interno è sorvegliato dallo stesso programma che potrebbe
avere il bug; quello esterno no.

## Privacy

`config.toml`, `state/`, `findings/`, i profili e il profilo browser sono in
`.gitignore`. Nessun dato personale sta nel codice, e un test lo verifica.
```

- [ ] **Step 6: Primo giro vero, con un solo post**

Mettere temporaneamente `posts_per_run = 1` in `config.toml`, poi:

```bash
winnow collect && winnow status && cat findings/$(date +%F).json
```

Expected: un post processato, entità con verifica, `spend_usd` sotto `0.05`.
Poi rimettere `posts_per_run = 8`.

- [ ] **Step 7: Schedulazione notturna**

```bash
crontab -e
```

Aggiungere (l'ora dispari è deliberata: un giro alle 03:00 spaccate ogni notte è un pattern):

```
17 3 * * * cd ~/progetti/winnow && \
  /usr/bin/env ANTHROPIC_API_KEY=... .venv/bin/winnow collect \
  >> state/cron.log 2>&1
```

⚠️ La chiave nel crontab è leggibile da chi ha accesso all'account. Alternativa
migliore: metterla in un file letto solo dall'utente e caricarla nel comando.

- [ ] **Step 8: Commit**

```bash
git add winnow/cli.py README.md tests/test_cli.py
git commit -m "feat: CLI, README e schedulazione notturna"
```

---

## Come si genera il recap (non è codice)

Il giudice non si implementa: è una sessione Claude Code. Ogni domenica:

> Leggi `findings/` dell'ultima settimana e `profiles/<tuo>.md`. Applica il
> criterio a due corsie della spec §6. Scrivi il recap nel formato della spec §7:
> intestazione con costo, **un** commento sul mucchio, poi le righe tenute con
> cosa è / è vivo / cosa tocca, e gli scarti in una riga ciascuno.

Se un domani il giudice diventa un modello locale, cambia solo chi legge quel
prompt. Il raccoglitore non cambia di una riga.

## Verifica finale

- [ ] `pytest tests/ -v` — tutti verdi
- [ ] `git status` pulito, e `git ls-files` **non** contiene `config.toml`, `state/`, `findings/`, `browser-profile/`
- [ ] `winnow collect` due volte di fila: il secondo giro processa **zero** post (dedup funziona)
- [ ] Mettere `halt_eur_week = 0.001` in config, lanciare `collect`: deve fermarsi e scrivere `HALTED`. Poi `winnow reset-halt` e rimettere il valore vero.
