{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Let the page drag its own window. Nothing else: the engine is reached over HTTP, not over Tauri's IPC, so no other capability has any reason to exist here.",
  "windows": ["main"],
  "remote": {
    "urls": ["http://127.0.0.1:*", "http://localhost:*"]
  },
  "permissions": [
    "core:window:allow-start-dragging",
    "core:window:allow-internal-toggle-maximize"
  ]
}
